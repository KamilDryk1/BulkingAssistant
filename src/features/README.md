# Feature modules

Domain types, pure business rules, query hooks, and mutations live in one folder per domain. UI composition belongs in `src/screens`; reusable visual primitives belong in `src/components`.
